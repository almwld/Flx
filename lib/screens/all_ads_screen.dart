import 'package:flutter/material.dart';
import '../widgets/custom_app_bar.dart';
class AllAdsScreen extends StatelessWidget { const AllAdsScreen({super.key}); @override Widget build(BuildContext context) => const Scaffold(appBar: CustomAppBar(title: 'جميع الإعلانات'), body: Center(child: Text('قيد الإعداد'))); }
