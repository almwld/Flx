import 'package:flutter/material.dart';
import '../widgets/custom_app_bar.dart';
class AddAdScreen extends StatelessWidget { const AddAdScreen({super.key}); @override Widget build(BuildContext context) => const Scaffold(appBar: CustomAppBar(title: 'إضافة إعلان'), body: Center(child: Text('قيد الإعداد'))); }
